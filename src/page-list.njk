---
title: Sitemap Page
subtitle:
pagination:
  data: collections.all
  size: 20
  alias: entries
layout: layouts/page.njk
permalink: /page-list/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber }}/{% endif %}
eleventyNavigation:
  parent: meta
  key: sitemap
  title: Sitemap
  order: 2
---

<div class="container mx-auto max-w-4xl">
{# <table >
  <thead>
    <tr>
      <th>URL</th>
      <th>Page Title</th>
    </tr>
  </thead>
  <tbody>
{%- for entry in entries %}
  <tr>
    <td><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></td>
    <td>{{ entry.data.title }}</td>
  </tr>
{%- endfor %}
  </tbody>
</table> #}

<div class="mt-10 p-5">
  <ul class="space-y-2">
{%- for entry in entries %}
    <li class="list-disc list-inside">
      <a href="{{ entry.url }}">
      <code>{{ entry.url }}</code>
      </a>
      <span class="font-bold mx-4">{{ entry.data.title }}</span>
    </li>
{%- endfor %}
  </ul>
</div>


</div>
